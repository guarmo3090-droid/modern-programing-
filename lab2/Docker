FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Копируем сам код
COPY . .

# По умолчанию запускаем наш скрипт
CMD ["python", "app.py"]
